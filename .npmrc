auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Local publish uses browser-based passkey (make publish / scripts/publish.sh).
# CI publish overrides this with npm_config_auth_type=legacy + NPM_TOKEN secret.
auth-type=web

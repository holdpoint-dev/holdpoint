# npm (used by npx at runtime)
legacy-peer-deps=false

# Local publish: add `auth-type=web` to ~/.npmrc for browser-based passkey.
# CI publish uses npm OIDC Trusted Publisher — no token or auth-type override needed.
